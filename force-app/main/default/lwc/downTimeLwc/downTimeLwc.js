import { LightningElement } from 'lwc';

export default class DownTimeLwc extends LightningElement {
    // Define a maintenance date or dynamically fetch it from an API if needed
    maintenanceDate = 'August 30, 2024, from 2:00 AM to 4:00 AM (PST)';
}